# todo
- [ ] new command: bin. adds the executive to the bin.
- [ ] similar to venv, alowing for a dev version of tools.

## dev version of tools
perhaps, we will have a $HOME/bin-dev that can be enabled:
- includes $HOME/bin-dev to $PATH
- adds something to the PS1

## modular PS1
as in enable and disable components, with weights.


